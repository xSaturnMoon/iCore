/*
 * HypervisorBridge.c
 *
 * Including the bridge header here causes the linker to pull in
 * Hypervisor.framework symbols when the .c compilation unit is linked.
 */
#include "hypervisor_bridge.h"
